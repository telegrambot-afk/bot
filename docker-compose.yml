version: '3.9'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_bot
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: sh -c "python3 manage.py bot"
